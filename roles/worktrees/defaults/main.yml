# SPDX-FileCopyrightText: 2026 Goethe-University Frankfurt – Institute for Digital Medicine and Clinical Data Science
# SPDX-FileCopyrightText: 2026 Mirian Brechtel <markus.katharina.brechtel@thengo.net>
#
# SPDX-License-Identifier: EUPL-1.2

---
worktrees_default_group: devops
worktrees_default_owner: root
# Base directory under which each project gets its shared work directory
# (`{{ worktrees_base }}/<project>`).
worktrees_base: /work
# Category folders to pre-create under the work directory, each with the same
# 3775 permissions as the work directory itself. Categories are just folders —
# there is no allowlist; the worktree hook creates a category folder on demand
# if it doesn't exist yet. These are the ones a fresh project starts with.
worktrees_default_categories:
  - feature      # new capabilities and behaviour
  - fix          # fixes to existing behaviour
  - hotfix       # urgent production fixes, branched from main/release, not develop
  - refactor     # structural changes with no behaviour change
  - test         # test-only changes
  - ci           # pipeline / workflow changes
  - chore        # tooling, dependencies, and housekeeping
  - docs         # documentation and pattern writing
  - update       # dependency bumps and version updates
  - experiment   # throwaway spikes and explorations
  - release      # release stabilization branches (gitflow-style; skip for trunk-based)
